{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SpiderSpeciesDatabase",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["id", "scientificName", "name", "continent", "venomLevel", "lat", "lng"],
    "properties": {
      "id": { "type": "integer" },
      "scientificName": { "type": "string" },
      "name": {
        "type": "object",
        "required": ["en", "tr"],
        "properties": { "en": { "type": "string" }, "tr": { "type": "string" } }
      },
      "continent": { "type": "string" },
      "venomLevel": { "type": "string", "enum": ["safe", "warn", "danger"] },
      "lat": { "type": "number" },
      "lng": { "type": "number" }
    }
  }
}